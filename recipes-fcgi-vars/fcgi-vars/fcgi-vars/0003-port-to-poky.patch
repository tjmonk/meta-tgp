From e2dcb700b967776bcb933d70279b16f066f7ba46 Mon Sep 17 00:00:00 2001
From: Ubuntu <ubuntu@ip-172-31-5-36.us-west-2.compute.internal>
Date: Sun, 18 Apr 2021 03:05:59 +0000
Subject: [PATCH] port to poky

---
 CMakeLists.txt  |   1 -
 src/fcgi.c      | 343 -----------------------------------------------
 src/fcgi_vars.c | 344 +++++++++++++++++++++++++++++++++++++++++++++---
 3 files changed, 329 insertions(+), 359 deletions(-)
 delete mode 100644 src/fcgi.c

diff --git a/CMakeLists.txt b/CMakeLists.txt
index 4b24d16..6c97b5a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -12,7 +12,6 @@ find_library( LIB_FCGI fcgi REQUIRED )
 
 add_executable( ${PROJECT_NAME}
 	src/fcgi_vars.c
-    src/fcgi.c
 )
 
 target_include_directories( ${PROJECT_NAME}
diff --git a/src/fcgi.c b/src/fcgi.c
deleted file mode 100644
index f55feae..0000000
--- a/src/fcgi.c
+++ /dev/null
@@ -1,343 +0,0 @@
-/*==============================================================================
-   Copyright (C) Trevor Monk - All Rights Reserved
-   Unauthorized copying of this file, via any medium is strictly prohibited
-   Proprietary and confidential
-   Written by Trevor Monk <tjmonk@gmail.com>, Apr 2021
- =============================================================================*/
-
-/*!
- * @defgroup fcgi fcgi
- * @brief Fast CGI Processing functions
- * @{
- */
-
-/*============================================================================*/
-/*!
-@file fcgi.c
-
-    FCGI Processing functions
-
-    The fcgi processing functions are a collection of I/O functions
-    which handle FCGI requests and output FCGI data
-
-*/
-/*============================================================================*/
-
-/*==============================================================================
-        Includes
-==============================================================================*/
-
-#include <fcgi_stdio.h>
-#include <string.h>
-#include <stdlib.h>
-#include <errno.h>
-#include "fcgi.h"
-
-/*==============================================================================
-        Private definitions
-==============================================================================*/
-
-
-/*==============================================================================
-        Private file scoped variables
-==============================================================================*/
-
-/*==============================================================================
-        Private function declarations
-==============================================================================*/
-
-static HandlerFunction GetHandlerFunction( char *method,
-                                           FCGIHandler *pFCGIHandlers,
-                                           size_t numHandlers );
-
-/*==============================================================================
-        Private function definitions
-==============================================================================*/
-
-/*============================================================================*/
-/*  ProcessRequests                                                           */
-/*!
-    Process incoming Fast CGI requests
-
-    The ProcessRequests function waits for incoming FCGI requests
-    and processes them according to their request method.
-    Typically this function will not exit, as doing so will terminate
-    the FCGI interface.
-
-    @param[in]
-        pState
-            pointer to the FCGIVars state object
-
-    @param[in]
-        pFCGIHandlers
-            pointer to an array of FCGIHandler objects which link method
-            names (eg GET, POST) with their method handling functions.
-
-    @param[in]
-        numHandlers
-            number of handlers in the array of FCGIHandler objects
-
-    @retval EOK request processed successfully
-    @retval EINVAL invalid arguments
-
-*//*
-    REVISION HISTORY:
-
-    Version: 1.0    07-Apr-2021     By: Trevor Monk
-        - created
-
-==============================================================================*/
-int ProcessRequests( FCGIVarsState *pState,
-                     FCGIHandler *pFCGIHandlers,
-                     size_t numHandlers )
-{
-    int result = EINVAL;
-    char *method;
-    HandlerFunction fn = NULL;
-
-    if ( ( pState != NULL ) &&
-         ( pFCGIHandlers != NULL ) &&
-         ( numHandlers > 0 ) )
-    {
-        /* wait for an FCGI request */
-        while( FCGI_Accept() >= 0 )
-        {
-            /* check the request method */
-            method = getenv("REQUEST_METHOD");
-            if ( method != NULL )
-            {
-                /* get the handler associated with the method */
-                fn = GetHandlerFunction( method, pFCGIHandlers, numHandlers );
-                if ( fn != NULL )
-                {
-                    /* invoke the handler */
-                    result = fn( pState );
-                }
-            }
-        }
-    }
-
-    return result;
-}
-
-/*============================================================================*/
-/*  GetHandlerFunction                                                        */
-/*!
-    Get the handler function for the specified method
-
-    The GetHandlerFunction function looks up the processing function
-    associated with the specified HTTP method.
-
-    The handler functions are passed in via the pFCGIHandler pointer
-
-    @param[in]
-        method
-            pointer to the method name, eg "GET", "POST"
-
-    @param[in]
-        pFCGIHandlers
-            pointer to the FCGI method handling functions
-
-    @param[in]
-        numHandlers
-            number of handlers in the method handling function array pointed
-            to by pFCGIHandler
-
-    @retval pointer to the method handler
-    @retval NULL no method handler could be found
-
-*//*
-    REVISION HISTORY:
-
-    Version: 1.0    07-Apr-2021     By: Trevor Monk
-        - created
-
-==============================================================================*/
-static HandlerFunction GetHandlerFunction( char *method,
-                                           FCGIHandler *pFCGIHandlers,
-                                           size_t numHandlers )
-{
-    size_t i;
-    FCGIHandler *pFCGIHandler;
-    HandlerFunction fn = NULL;
-
-    if ( ( method != NULL ) &&
-         ( pFCGIHandlers != NULL ) &&
-         ( numHandlers > 0 ) )
-    {
-        /* iterate through the FCGI method handlers */
-        for ( i = 0; i < numHandlers ; i++ )
-        {
-            /* get a pointer to the current method handler */
-            pFCGIHandler = &pFCGIHandlers[i];
-            if ( pFCGIHandler != NULL )
-            {
-                /* check if it matches the REQUEST_METHOD or the
-                 * wild card */
-                if ( ( strcmp( pFCGIHandler->handler, method ) == 0 ) ||
-                     ( strcmp( pFCGIHandler->handler, "*" ) == 0 ) )
-                {
-                    /* get a pointer to the handler function */
-                    fn = pFCGIHandler->fn;
-                    break;
-                }
-            }
-        }
-    }
-
-    return fn;
-}
-
-/*============================================================================*/
-/*  SendVarsHeader                                                            */
-/*!
-    Send a variable query response header
-
-    The SendVarsHeader function sends a variable response header
-
-    @retval EOK response sent successfully
-    @retval EINVAL invalid arguments
-
-*//*
-    REVISION HISTORY:
-
-    Version: 1.0    07-Apr-2021     By: Trevor Monk
-        - created
-
-==============================================================================*/
-void SendVarsHeader( void )
-{
-    /* output the response header */
-    printf("Status: OK\r\n");
-    printf("Content-Type: application/json\r\n\r\n");
-
-    /* output the response body */
-    printf("{\"values\" : [");
-
-}
-
-/*============================================================================*/
-/*  OutputJSONVar                                                             */
-/*!
-    Output a variable JSON object with a prefix
-
-    The OutputJSONVar function prints a variable JSON object with a prefix.
-    The prefix is intended to be either a space, or a comma so this
-    function can be used to output a list of variables and prepend (or not)
-    a comma.
-
-    The object will be similar to the following:
-
-    { "name" : "some name", "value" : "some value" }
-
-    @param[in]
-        name
-            name of the variable
-
-    @param[in]
-        value
-            value of the variable as a string
-
-    @retval EOK the JSON object was output
-    @retval EINVAL invalid arguments
-
-*//*
-    REVISION HISTORY:
-
-    Version: 1.0    07-Apr-2021     By: Trevor Monk
-        - created
-
-==============================================================================*/
-int OutputJSONVar( char prefix, char *name, char *value )
-{
-    int result = EINVAL;
-
-    if ( ( name != NULL ) &&
-         ( value != NULL ) )
-    {
-        printf( "%c{ \"name\": \"%s\", \"value\" : \"%s\" }",
-                prefix,
-                name,
-                value );
-
-        result = EOK;
-    }
-
-    return result;
-}
-
-/*============================================================================*/
-/*  SendVarsFooter                                                            */
-/*!
-    Send a variable query response footer
-
-    The SendVarsFooter function sends a variable response footer
-
-    @param[in]
-        count
-            count of the number of variables in the payload
-
-*//*
-    REVISION HISTORY:
-
-    Version: 1.0    07-Apr-2021     By: Trevor Monk
-        - created
-
-==============================================================================*/
-void SendVarsFooter( int count )
-{
-    printf("],\"count\": %d }\n", count );
-}
-
-/*============================================================================*/
-/*  ErrorResponse                                                             */
-/*!
-    Send an error response
-
-    The ErrorResponse function sends an error response to the client
-    using the Status header, and the status code and error description
-    in a JSON object.
-
-    @param[in]
-        status
-            status response code
-
-    @param[in]
-        description
-            status response description
-
-    @retval EOK the response was sent
-    @retval EINVAL invalid arguments
-
-*//*
-    REVISION HISTORY:
-
-    Version: 1.0    07-Apr-2021     By: Trevor Monk
-        - created
-
-==============================================================================*/
-int ErrorResponse( int status,  char *description )
-{
-    int result = EINVAL;
-
-    if ( description != NULL )
-    {
-        /* output header */
-        printf("Status: %d %s\r\n");
-        printf("Content-Type: application/json\r\n\r\n");
-
-        /* output body */
-        printf("{\"status\": %d, \"description\" : \"%s\"}",
-                status,
-                description );
-
-        result = EOK;
-
-    }
-
-    return result;
-}
-
-
-/*! @>
- * end of fcgi group */
diff --git a/src/fcgi_vars.c b/src/fcgi_vars.c
index 3a78cff..3a6bc5f 100644
--- a/src/fcgi_vars.c
+++ b/src/fcgi_vars.c
@@ -28,8 +28,6 @@
         Includes
 ==============================================================================*/
 
-#include <stdio.h>
-#include <fcgi_stdio.h>
 #include <string.h>
 #include <stdbool.h>
 #include <errno.h>
@@ -41,7 +39,7 @@
 #include <varserver/varserver.h>
 #include <varserver/varcache.h>
 #include <varserver/varfp.h>
-#include "fcgi.h"
+#include <fcgi_stdio.h>
 
 /*==============================================================================
         Private definitions
@@ -87,6 +85,18 @@ typedef struct _queryFunc
 
 } QueryFunc;
 
+/*! Handler function */
+typedef int (*HandlerFunction)(FCGIVarsState *);
+
+/*! FCGI Handler function */
+typedef struct _fcgi_handler
+{
+    /*! handler name */
+    char *handler;
+
+    /*! handler function */
+    HandlerFunction fn;
+} FCGIHandler;
 
 /*==============================================================================
         Private function declarations
@@ -96,6 +106,10 @@ void main(int argc, char **argv);
 static int InitState( FCGIVarsState *pState );
 static int ProcessOptions( int argC, char *argV[], FCGIVarsState *pState );
 static void usage( char *cmdname );
+static int ProcessRequests( FCGIVarsState *pState,
+                            FCGIHandler *pFCGIHandlers,
+                            size_t numHandlers );
+
 static int ProcessGETRequest( FCGIVarsState *pState );
 static int ProcessPOSTRequest( FCGIVarsState *pState );
 static int GetPOSTData( FCGIVarsState *pState, size_t length );
@@ -109,6 +123,15 @@ static int OutputVar( VAR_HANDLE hVar, void *arg );
 static void SetupTerminationHandler( void );
 static void TerminationHandler( int signum, siginfo_t *info, void *ptr );
 
+static HandlerFunction GetHandlerFunction( char *method,
+                                           FCGIHandler *pFCGIHandlers,
+                                           size_t numHandlers );
+
+static int OutputJSONVar( char prefix, char *name, char *value );
+static void SendVarsHeader( void );
+static void SendVarsFooter( int count );
+static int ErrorResponse( int status,  char *description );
+
 /*==============================================================================
         Private file scoped variables
 ==============================================================================*/
@@ -161,6 +184,12 @@ void main(int argc, char **argv)
     /* initialize the FCGI Vars state */
     InitState( &state );
 
+    /* process FCGI requests */
+  /*  ProcessRequests( &state,
+                     methodHandlers,
+		     sizeof(methodHandlers) / sizeof(FCGIHandler) );
+  */
+
     /* set up the termination handler */
     SetupTerminationHandler();
 
@@ -205,22 +234,18 @@ void main(int argc, char **argv)
 }
 
 /*============================================================================*/
-/*  main                                                                      */
+/*  InitState                                                                 */
 /*!
-    Main entry point for the fcgi_vars application
+    Initialize the FCGIVars state 
 
-    The main function starts the fcgi_vars application
+    The InitState function initializes the FCGIVars state object
 
     @param[in]
-        argc
-            number of arguments on the command line
-            (including the command itself)
+        pState 
+            pointer to the FCGIVarsState object to initialize
 
-    @param[in]
-        argv
-            array of pointers to the command line arguments
-
-    @return none
+    @retval EOK the FCGIVarsState object was successfully initialized
+    @retval EINVAL invalid arguments
 
 *//*
     REVISION HISTORY:
@@ -351,6 +376,140 @@ static int ProcessOptions( int argC, char *argV[], FCGIVarsState *pState )
     return result;
 }
 
+/*============================================================================*/
+/*  ProcessRequests                                                           */
+/*!
+    Process incoming Fast CGI requests
+
+    The ProcessRequests function waits for incoming FCGI requests
+    and processes them according to their request method.
+    Typically this function will not exit, as doing so will terminate
+    the FCGI interface.
+
+    @param[in]
+        pState
+            pointer to the FCGIVars state object
+
+    @param[in]
+        pFCGIHandlers
+            pointer to an array of FCGIHandler objects which link method
+            names (eg GET, POST) with their method handling functions.
+
+    @param[in]
+        numHandlers
+            number of handlers in the array of FCGIHandler objects
+
+    @retval EOK request processed successfully
+    @retval EINVAL invalid arguments
+
+*//*
+    REVISION HISTORY:
+
+    Version: 1.0    07-Apr-2021     By: Trevor Monk
+        - created
+
+==============================================================================*/
+static int ProcessRequests( FCGIVarsState *pState,
+                            FCGIHandler *pFCGIHandlers,
+                            size_t numHandlers )
+{
+    int result = EINVAL;
+    char *method;
+    HandlerFunction fn = NULL;
+
+    if ( ( pState != NULL ) &&
+         ( pFCGIHandlers != NULL ) &&
+         ( numHandlers > 0 ) )
+    {
+        /* wait for an FCGI request */
+        while( FCGI_Accept() >= 0 )
+        {
+            /* check the request method */
+            method = getenv("REQUEST_METHOD");
+            if ( method != NULL )
+            {
+                /* get the handler associated with the method */
+                fn = GetHandlerFunction( method, pFCGIHandlers, numHandlers );
+                if ( fn != NULL )
+                {
+                    /* invoke the handler */
+                    result = fn( pState );
+                }
+            }
+        }
+    }
+
+    return result;
+}
+
+/*============================================================================*/
+/*  GetHandlerFunction                                                        */
+/*!
+    Get the handler function for the specified method
+
+    The GetHandlerFunction function looks up the processing function
+    associated with the specified HTTP method.
+
+    The handler functions are passed in via the pFCGIHandler pointer
+
+    @param[in]
+        method
+            pointer to the method name, eg "GET", "POST"
+
+    @param[in]
+        pFCGIHandlers
+            pointer to the FCGI method handling functions
+
+    @param[in]
+        numHandlers
+            number of handlers in the method handling function array pointed
+            to by pFCGIHandler
+
+    @retval pointer to the method handler
+    @retval NULL no method handler could be found
+
+*//*
+    REVISION HISTORY:
+
+    Version: 1.0    07-Apr-2021     By: Trevor Monk
+        - created
+
+==============================================================================*/
+static HandlerFunction GetHandlerFunction( char *method,
+                                           FCGIHandler *pFCGIHandlers,
+                                           size_t numHandlers )
+{
+    size_t i;
+    FCGIHandler *pFCGIHandler;
+    HandlerFunction fn = NULL;
+
+    if ( ( method != NULL ) &&
+         ( pFCGIHandlers != NULL ) &&
+         ( numHandlers > 0 ) )
+    {
+        /* iterate through the FCGI method handlers */
+        for ( i = 0; i < numHandlers ; i++ )
+        {
+            /* get a pointer to the current method handler */
+            pFCGIHandler = &pFCGIHandlers[i];
+            if ( pFCGIHandler != NULL )
+            {
+                /* check if it matches the REQUEST_METHOD or the
+                 * wild card */
+                if ( ( strcmp( pFCGIHandler->handler, method ) == 0 ) ||
+                     ( strcmp( pFCGIHandler->handler, "*" ) == 0 ) )
+                {
+                    /* get a pointer to the handler function */
+                    fn = pFCGIHandler->fn;
+                    break;
+                }
+            }
+        }
+    }
+
+    return fn;
+}
+
 /*============================================================================*/
 /*  SendVarsResponse                                                          */
 /*!
@@ -384,7 +543,7 @@ static int SendVarsResponse( FCGIVarsState *pState )
         /* get a pointer to the variable cache */
         pCache = pState->pVarCache;
 
-        SendVarsHeader();
+	SendVarsHeader();
 
         /* initialize the output count */
         pState->outputCount = 0;
@@ -438,6 +597,10 @@ static int ProcessGETRequest( FCGIVarsState *pState )
         {
             result = SendVarsResponse( pState );
         }
+	else
+	{
+	    result = ErrorResponse( 400, "Bad request" );
+	}
     }
 
     return result;
@@ -1021,5 +1184,156 @@ static void TerminationHandler( int signum, siginfo_t *info, void *ptr )
     VARFP_Close( state.pVarFP );
 }
 
+/*============================================================================*/
+/*  SendVarsHeader                                                            */
+/*!
+    Send a variable query response header
+
+    The SendVarsHeader function sends a variable response header
+
+    @retval EOK response sent successfully
+    @retval EINVAL invalid arguments
+
+*//*
+    REVISION HISTORY:
+
+    Version: 1.0    07-Apr-2021     By: Trevor Monk
+        - created
+
+==============================================================================*/
+static void SendVarsHeader( void )
+{
+    /* output the response header */
+    printf("Status: 200 OK\r\n");
+    printf("Content-Type: application/json\r\n\r\n");
+
+    /* output the response body */
+    printf("{\"values\" : [");
+
+}
+
+/*============================================================================*/
+/*  OutputJSONVar                                                             */
+/*!
+    Output a variable JSON object with a prefix
+
+    The OutputJSONVar function prints a variable JSON object with a prefix.
+    The prefix is intended to be either a space, or a comma so this
+    function can be used to output a list of variables and prepend (or not)
+    a comma.
+
+    The object will be similar to the following:
+
+    { "name" : "some name", "value" : "some value" }
+
+    @param[in]
+        name
+            name of the variable
+
+    @param[in]
+        value
+            value of the variable as a string
+
+    @retval EOK the JSON object was output
+    @retval EINVAL invalid arguments
+
+*//*
+    REVISION HISTORY:
+
+    Version: 1.0    07-Apr-2021     By: Trevor Monk
+        - created
+
+==============================================================================*/
+static int OutputJSONVar( char prefix, char *name, char *value )
+{
+    int result = EINVAL;
+
+    if ( ( name != NULL ) &&
+         ( value != NULL ) )
+    {
+        printf( "%c{ \"name\": \"%s\", \"value\" : \"%s\" }",
+                prefix,
+                name,
+                value );
+
+        result = EOK;
+    }
+
+    return result;
+}
+
+/*============================================================================*/
+/*  SendVarsFooter                                                            */
+/*!
+    Send a variable query response footer
+
+    The SendVarsFooter function sends a variable response footer
+
+    @param[in]
+        count
+            count of the number of variables in the payload
+
+*//*
+    REVISION HISTORY:
+
+    Version: 1.0    07-Apr-2021     By: Trevor Monk
+        - created
+
+==============================================================================*/
+static void SendVarsFooter( int count )
+{
+    printf("],\"count\": %d }\n", count );
+}
+
+/*============================================================================*/
+/*  ErrorResponse                                                             */
+/*!
+    Send an error response
+
+    The ErrorResponse function sends an error response to the client
+    using the Status header, and the status code and error description
+    in a JSON object.
+
+    @param[in]
+        status
+            status response code
+
+    @param[in]
+        description
+            status response description
+
+    @retval EOK the response was sent
+    @retval EINVAL invalid arguments
+
+*//*
+    REVISION HISTORY:
+
+    Version: 1.0    07-Apr-2021     By: Trevor Monk
+        - created
+
+==============================================================================*/
+static int ErrorResponse( int status,  char *description )
+{
+    int result = EINVAL;
+
+    if ( description != NULL )
+    {
+        /* output header */
+        printf("Status: %d %s\r\n", status, description);
+        printf("Content-Type: application/json\r\n\r\n");
+
+        /* output body */
+        printf("{\"status\": %d, \"description\" : \"%s\"}",
+                status,
+                description );
+	
+        result = EOK;
+
+    }
+
+    return result;
+}
+
+
 /*! @>
  * end of fcgi_vars group */
