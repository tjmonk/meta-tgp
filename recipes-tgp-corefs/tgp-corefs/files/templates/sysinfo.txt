-------------------------------------------------------------

This is a test
The value of /sys/test/a is ${/sys/test/a} and the value of
/sys/test/b is ${/sys/test/b}.
We can even print floats: /sys/test/f is ${/sys/test/f} and
we can print strings too: /sys/test/c is:
${/sys/test/c}
The uptime is ${/SYS/INFO/UPTIME}
The MAC address is ${/SYS/NETWORK/MAC}

-------------------------------------------------------------
